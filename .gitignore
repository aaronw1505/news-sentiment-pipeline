# Python
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# Secrets
.env

# DuckDB local dev database.
# CI (GitHub Actions) publishes the canonical database via `git add -f`,
# so the local working copy stays out of version control.
data/*.duckdb
data/*.duckdb.wal

# dbt
dbt_project/target/
dbt_project/dbt_packages/
dbt_project/logs/
logs/

# OS / editor
.DS_Store
.idea/
.vscode/
